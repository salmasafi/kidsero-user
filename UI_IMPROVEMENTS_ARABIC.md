# تحسينات واجهة المستخدم - إزالة N/A

## المشكلة
كانت الواجهة تعرض "N/A" للبيانات المفقودة (الصف والفصل) مما يجعل المظهر غير احترافي.

## الحل المطبق

### 1. تحديث نموذج الطفل (Child Model)
**الملف**: `lib/features/children/model/child_model.dart`

**التحسينات**:
- إضافة حقل `classroom` منفصل عن `schoolName`
- إضافة حقل `organization` لتخزين معلومات المدرسة/المنظمة
- إضافة خصائص مساعدة (getters):
  - `displayGrade` - يعرض الصف أو نص فارغ
  - `displayClassroom` - يعرض الفصل أو نص فارغ
  - `displaySchoolName` - يعرض اسم المدرسة من المنظمة أو schoolName
  - `hasCompleteInfo` - للتحقق من وجود بيانات كاملة

**مثال على البيانات من API**:
```json
{
  "id": "...",
  "name": "Mariam",
  "grade": "12",
  "classroom": "1B",
  "organization": {
    "name": "KGC"
  }
}
```

### 2. تحديث بطاقة الطفل (Child Card)
**الملف**: `lib/core/widgets/child_card.dart`

**التحسينات**:
- تغيير `grade` و `classroom` من required إلى optional
- إضافة `schoolName` كخيار إضافي
- منطق عرض ذكي:
  - إذا كان هناك صف أو فصل → يعرضهما مع أيقونة المدرسة
  - إذا لم يكن هناك صف/فصل لكن هناك اسم مدرسة → يعرض اسم المدرسة مع أيقونة المبنى
  - إذا لم يكن هناك أي بيانات → يحافظ على المسافات فقط بدون عرض "N/A"

**منطق العرض**:
```dart
if (hasGradeOrClassroom) {
  // عرض: "الصف الثالث • 3/أ"
} else if (schoolName != null) {
  // عرض: "Franciscan Schools"
} else {
  // مسافة فارغة فقط
}
```

### 3. تحديث صفحة الرحلات
**الملف**: `lib/features/rides/ui/screens/rides_screen.dart`

**التغييرات**:
- استخدام `child.displayGrade` بدلاً من `child.grade ?? 'N/A'`
- استخدام `child.displayClassroom` بدلاً من `child.schoolName ?? 'N/A'`
- إضافة `child.displaySchoolName` كخيار احتياطي
- تمرير `null` بدلاً من النصوص الفارغة

### 4. تحديث شاشة جدول الطفل
**الملف**: `lib/features/rides/ui/screens/child_schedule_screen.dart`

**التحسينات**:
- تغيير `grade` و `classroom` إلى optional
- إضافة `schoolName` كمعامل
- إضافة دالة `_buildChildInfoText()` لبناء النص بذكاء
- عرض المعلومات فقط إذا كانت متوفرة

## النتيجة

### قبل التحسين:
```
الاسم: أحمد
🏫 N/A • N/A
```

### بعد التحسين:

**حالة 1 - بيانات كاملة**:
```
الاسم: Mariam
🏫 12 • 1B
```

**حالة 2 - صف فقط**:
```
الاسم: أحمد
🏫 الصف الثالث
```

**حالة 3 - فصل فقط**:
```
الاسم: سارة
🏫 3/أ
```

**حالة 4 - اسم مدرسة فقط**:
```
الاسم: محمد
🏢 Franciscan Schools
```

**حالة 5 - لا توجد بيانات**:
```
الاسم: علي
[مسافة فارغة - بدون N/A]
```

## الملفات المعدلة
1. `lib/features/children/model/child_model.dart` - نموذج البيانات
2. `lib/core/widgets/child_card.dart` - بطاقة عرض الطفل
3. `lib/features/rides/ui/screens/rides_screen.dart` - الصفحة الرئيسية
4. `lib/features/rides/ui/screens/child_schedule_screen.dart` - شاشة الجدول

## الفوائد
✅ واجهة أكثر احترافية
✅ لا يوجد نص "N/A" مزعج
✅ عرض ذكي للبيانات المتاحة
✅ دعم أفضل لهيكل البيانات من API
✅ مرونة في التعامل مع البيانات المفقودة
